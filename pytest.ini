[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
markers =
    integration: exercises the app against the disposable integration database
    ws: covers Socket.IO chat behavior
    jobs: covers background job execution behavior
    e2e: covers browser-level end-to-end flows in Chromium
    dependency: validates dependency integrity and prevents dependency hell
